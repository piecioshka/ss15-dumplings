define([

], function () {
    'use strict';

    var Configuration = {
        WORLD_WIDTH: 1600,  // 32 * 50
        WORLD_HEIGHT: 800,  // 32 * 25,

        WINDOW_WIDTH: window.innerWidth,
        WINDOW_HEIGHT: window.innerHeight
    };

    return Configuration;
});
